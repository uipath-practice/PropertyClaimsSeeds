Upstream defects you will actually meet, described by behaviour: what you will see, and what to do instead.

No bug numbers are used as instructions. An entry keyed to a defect id becomes a lie the day the defect closes,
and you would have no way to tell.
